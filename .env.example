DATABASE_URL="postgresql://bela:bela@localhost:5432/bela_agenda?schema=public"
APP_URL="http://localhost:3000"
RESEND_API_KEY=""
EMAIL_FROM="Santa Agenda <agenda@seudominio.com.br>"
# Deixe vazio até configurar um proxy que sobrescreva este cabeçalho com o IP real.
# Sem configuração, requisições compartilham o limite de rede (mais restritivo).
TRUSTED_CLIENT_IP_HEADER=""
